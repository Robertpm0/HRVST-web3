import react from "react";
import WebView from "react-native-webview";
import { Component } from "react";

export default class TransakButton extends Component {
    state = { showWebView: false }

    render() {
        if (this.state.showWebView) return 


    }
}